node_modules
.next
out
.env
.env.local
.env.*.local
next-env.d.ts
*.tsbuildinfo
.DS_Store

# Large sample schedule exports (kept locally as reference; fixtures live in tests/)
/*.xml

# Real-data parser fixtures kept local only (synthetic minimal.xml stays tracked)
/tests/fixtures/cath-ir-baseline.xml

# Brainstorming visual-companion session files
.superpowers/
